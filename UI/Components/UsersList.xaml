<UserControl x:Class="Parmigiano.UI.Components.UsersList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Parmigiano.UI.Components"
             xmlns:vm="clr-namespace:Parmigiano.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">

    <UserControl.DataContext>
        <vm:UsersViewModel />
    </UserControl.DataContext>

    <Border BorderThickness="0,0,1,0"
            BorderBrush="#555"
            Background="#111111">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <local:UserMeCard Grid.Row="0" />

            <TextBlock Text="Сообщения" Grid.Row="1"
                       Foreground="White"
                       FontWeight="SemiBold"
                       FontSize="17"
                       Margin="13,10,10,13"/>

            <ListBox ItemsSource="{Binding Users}" Grid.Row="2"
                        Background="#111111"
                        BorderThickness="0"
                        PreviewMouseWheel="ListBox_PreviewMouseWheel"
                        ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                        ScrollViewer.VerticalScrollBarVisibility="Hidden"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedUser}">

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>

                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#222831"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#393E46"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="10">
                            <Ellipse Width="52" Height="54" StrokeThickness="1" Stroke="#222">
                                <Ellipse.Fill>
                                    <ImageBrush ImageSource="{Binding Avatar}" Stretch="UniformToFill"/>
                                </Ellipse.Fill>
                            </Ellipse>

                            <StackPanel Margin="15,0,0,0">
                                <TextBlock Text="{Binding Username}" 
                                            Foreground="White"
                                            FontWeight="SemiBold"
                                            FontSize="15"/>
                                <TextBlock Text="{Binding LastMessage}" 
                                            Foreground="#888" 
                                            Margin="0,5,0,0"
                                            FontSize="13" 
                                            TextTrimming="CharacterEllipsis"
                                            MaxWidth="160"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</UserControl>
